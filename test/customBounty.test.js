const { expect } = require("chai");
const { ethers } = require("hardhat");

describe.skip("FreelanceBountyBoard - Access Control Test", function () {
  it("Should prevent non-employer from approving and paying a bounty", async function () {
    // Skipped
  });
});